## 多态

## 1. 多态的定义及实现

### 1.1 多态的构成条件

> 多态是在不同继承关系的**类对象**，去调用同一函数，产生了不同的行为。比如Student继承了 Person。Person对象买票全价，Student对象买票半价。
>
> 在继承中要构成多态还有两个条件：
>
> 1. 必须通过**==基类==的指针或者引用**调用虚函数
> 2. 被调用的函数必须是虚函数，且派生类必须对基类的虚函数进行重写

### 1.2 虚函数

> 虚函数：即被virtual关键字修饰的类成员函数
>
> ```cpp
> class Person {
> public:
> 	virtual void BuyTicket() { cout << "买票-全价" << endl; }
> };
> ```

### 1.3 虚函数的重写

> 虚函数的重写(覆盖)：**派生类中有一个跟基类完全相同的虚函数(即派生类虚函数与基类虚函数的返回值类型、函数名字、参数列表完全相同**)，称子类的虚函数重写了基类的虚函数。
>
> 满足多态：跟调用的对象的类型无关，跟指向对象有关，指向哪个对象调用哪个对象的虚函数
> 不满足多态：跟调用的对象的类型有关，类型是什么调用就是谁的虚函数
>
> ```cpp
> class Person {
> public:
> 	virtual void BuyTicket() { cout << "买票-全价" << endl; }
> };
> 
> class Student : public Person {
> public:
> 	virtual void BuyTicket() { cout << "买票-半价" << endl; }
> };
> 
> void Func(Person& p)
> {
> 	p.BuyTicket();
> }
> 
> int main()
> {
> 	Person ps;
> 	Student st;
> 	Func(ps);
> 	Func(st);
> 	return 0;
> }
> ```
>
> **虚函数重写的例外**
>
> 1. **协变(基类与派生类虚函数返回值类型不同)**
> 	基类虚函数返回基类对象的指 针或者引用，派生类虚函数返回派生类对象的指针或者引用时，称为协变。
> 	派生类函数返回值可以与基类函数返回值不同，但必须是对应类的指针或者引用
>
> 	```cpp
> 	class A {};
> 	class B : public A {};
> 		
> 	class Person {
> 	public:
> 		virtual A* f() { return new A; }
> 	};
> 		
> 	class Student : public Person {
> 	public:
> 		virtual B* f() { return new B; }
> 	};
> 	```
>
> 2. 派生类的虚函数在不加virtual关键字时，虽然也可以构成重写(因 为继承后基类的虚函数被继承下来了在派生类依旧保持虚函数属性),但是该种写法不是很规范，不建议这样使用。
>
> 3. **析构函数的重写(基类与派生类析构函数的名字不同)**
>
> 	```cpp
> 	class Person {
> 	public:
> 		~Person() { cout << "~Person()" << endl; }
> 	};
> 		
> 	class Student : public Person {
> 	public:
> 		~Student() { cout << "~Student()" << endl; }
> 	};
> 	// 只有派生类Student的析构函数重写了Person的析构函数，下面的delete对象调用析构函
> 	// 数，才能构成多态，才能保证p1和p2指向的对象正确的调用析构函数。
> 		
> 	int main() {
> 		Person* p1 = new Person;
> 		delete p1;
> 		
> 		Person* p2 = new Student;
> 		delete p2;
> 		
> 		return 0;
> 	}
> 		
> 	// 这里如果不把析构函数写成虚函数，那么就不能构成多态，调用析构函数的时候，
> 	// 根据类型来调用，就会调用两个Person的析构
> 	```
>
> 	虚函数重写后，与原函数相比，只改变了函数体，而函数传参、函数名、函数返回没有变，因此函数传参时的缺省参数也使用原函数的。

### 1.4 override & final(C++11)

> 1. **final：修饰虚函数，表示该虚函数不能再被重写**
>
> 	```cpp
> 	class Car
> 	{
> 	public:
> 		virtual void Drive() final {}
> 	};
> 	
> 	class Benz :public Car
> 	{
> 	public:
> 		virtual void Drive() { cout << "Benz-舒适" << endl; }
> 	    // 这里不能再重写Drive()
> 	};
> 	```
>
> 2. **override: 检查派生类虚函数是否重写了基类某个虚函数，如果没有重写编译报错。**
>
> 	```cpp
> 	class Car {
> 	public:
> 		virtual void Drive() {}
> 	};
> 	
> 	
> 	class Benz :public Car {
> 	public:
> 		virtual void Drive() override { cout << "Benz-舒适" << endl; }
> 	};
> 	```

### 1.5 重载、覆盖（重写）、隐藏（重定义）的对比

> <img src="../../../../AppData/Roaming/Typora/typora-user-images/image-20250211133014276.png" alt="image-20250211133014276" style="zoom:50%;" />
>
> 重写是为了重写函数实现
>
> 重定义要求更低

## 2. 抽象类

### 2.1 概念

> 在虚函数后面写上 =0 ，则这个函数为纯虚函数。**包含纯虚函数的类叫做抽象类（也叫接口类）**，抽象类不能实例化出对象。派生类继承后也不能实例化出对象，只有重写纯虚函数，派生类才能实例化出对象。**纯虚函数规范了派生类必须重写**，另外纯虚函数更体现出了接口继承。
>
> ```cpp
> // 抽象类
> class Car {
> public:
> 	virtual void Drive() = 0; // 不需要实现，纯虚函数
> };
> 
> class Benz : public Car {
> public:
> };
> 
> class BMW :public Car
> {
> public:
> 	virtual void Drive()
> 	{
> 		cout << "BMW-操控" << endl;
> 	}
> };
> 
> 
> // 纯虚函数的作用
> // 1、强制子类完成重写
> // 2、表示抽象的类型；抽象就是在现实中没有对应的实体的
> 
> int main() {
> 	// Car car; 抽象类不能实例化出对象
> 	
> 	// Benz ben; 没有重写的虚函数，Benz也无法实例化出对象
> 
> 	Car* p = new BMW;
> 	p->Drive(); // 可以正常调用
> 
> 	return 0;
> }
> ```

### 2.2 接口继承和实现继承

> 普通函数的继承是一种实现继承，派生类继承了基类函数，可以使用函数，继承的是**函数的实现**。虚函数的继承是一种接口继承，派生类继承的是基类虚函数的接口，目的是为了重写，达成多态，继承的是**接口**。所以如果不实现多态，不要把函数定义成虚函数。

## 3. 多态的原理

### 3.1 虚函数表

> ![image-20250211142128397](../../../../AppData/Roaming/Typora/typora-user-images/image-20250211142128397.png)
>
> b对象的大小是8bytes，**除了_b成员，还多一个__vfptr放在对象的前面(注意有些 平台可能会放到对象的最后面，这个跟平台有关)，对象中的这个指针我们叫做虚函数表指针(v代 表virtual，f代表function)**。
>
> ```cpp
> class Person {
> public:
> 	virtual void BuyTicket() { cout << "买票-全价" << endl; }
> 
> 	int _p = 1;
> };
> 
> class Student : public Person {
> public:
> 	virtual void BuyTicket() { cout << "买票-半价" << endl; }
> 
> 	int _s = 2;
> };
> 
> void Func(Person& p)
> {
> 	p.BuyTicket();
> }
> 
> 
> int main()
> {
> 	Person ps;
> 	Student st;
> 
> 	Func(ps);
> 	Func(st);
> 	return 0;
> }
> ```
>
> <img src="../../../../AppData/Roaming/Typora/typora-user-images/image-20250211144255521.png" alt="image-20250211144255521" style="zoom:50%;" />
>
> 如何实现的？
>
> 多态是在运行时到**指向的对象**的虚表中查找要调用的虚函数的地址
>
> ![image-20250211150315392](../../../../AppData/Roaming/Typora/typora-user-images/image-20250211150315392.png)
>
> call eax中存虚函数的指针。这里可以看出满足多态的调用，不是在编译时确定的，是运行起来以后到对象的中取找的。
> 下面的call就是直接调用函数
>
> vs中，虚函数实际是存在代码段的，对象中存储的是==虚函数表指针==

### 3.2动态绑定与静态绑定

> 1. 静态绑定又称为前期绑定(早绑定)，在**程序编译期间确定了程序的行为，也称为静态多态， **
> 	比如：函数重载
> 2. 动态绑定又称后期绑定(晚绑定)，是在**程序运行期间**，根据具体拿到的类型确定程序的具体行为，调用具体的函数，也称为动态多态。

## 4. 单继承和多继承的虚函数表

### 4.1 单继承中的虚函数表

> ```cpp
> class Base {
> public:
> 	virtual void func1() { cout << "Base::func1" << endl; }
> 	virtual void func2() { cout << "Base::func2" << endl; }
> private:
> 	int a;
> };
> 
> class Derive :public Base {
> public:
> 	virtual void func1() { cout << "Derive::func1" << endl; }
> 	virtual void func3() { cout << "Derive::func3" << endl; }
> 	virtual void func4() { cout << "Derive::func4" << endl; }
> private:
> 	int b;
> };
> ```
>
> 用调试器看的时候，发现找不到func3() 和 func4() ；写一个打印虚表的函数
>
> ```cpp
> // void(*)() 定义一个函数指针变量
> typedef void(*VF_PTR)();
> 
> // 打印虚表
> void PrintVfTable(VF_PTR* pTable) {
> 	for (size_t i = 0; pTable[i] != 0; ++i) {
> 		printf("vfTable[%d]:%p->", i, *(int*)pTable[i]);
> 		VF_PTR f = pTable[i];
> 		f();
> 	}
> }
> 
> 
> int main()
> {
> 	Base b;
> 	PrintVfTable((VF_PTR*)*(int*)&b);
> 
> 	cout << endl;
> 
> 	Derive d;
> 	PrintVfTable((VF_PTR*)*(int*)&d);
> 	// 取对象中前4个字节存的虚表指针
> 
> 	return 0;
> }
> 
>  // 思路：取出b、d对象的头4bytes，就是虚表的指针，前面我们说了虚函数表本质是一个存虚函数指针的指针数组，这个数组最后面放了一个nullptr
>  // 1.先取b的地址，强转成一个int*的指针
> // 2.再解引用取值，就取到了b对象头4bytes的值，这个值就是指向虚表的指针
> // 3.再强转成VF_PTR*，因为虚表就是一个存VF_PTR类型(虚函数指针类型)的数组。
> // 4.虚表指针传递给PrintVfTable进行打印虚表
> // 5.需要说明的是这个打印虚表的代码经常会崩溃，因为编译器有时对虚表的处理不干净，虚表最后面没有放nullptr，导致越界，这是编译器的问题。我们只需要点目录栏的-生成-清理解决方案，再编译就好了。
> ```
>
> ![image-20250211163711547](../../../../AppData/Roaming/Typora/typora-user-images/image-20250211163711547.png)

### 4.2 多继承的虚函数表

> ```cpp
> class Base1 {
> public:
> 	virtual void func1() { cout << "Base1::func1" << endl; }
> 	virtual void func2() { cout << "Base1::func2" << endl; }
> private:
> 	int b1;
> };
> 
> class Base2 {
> public:
> 	virtual void func1() { cout << "Base2::func1" << endl; }
> 	virtual void func2() { cout << "Base2::func2" << endl; }
> private:
> 	int b2;
> };
> 
> class Derive : public Base1, public Base2 {
> public:
> 	virtual void func1() { cout << "Derive::func1" << endl; }
> 	virtual void func3() { cout << "Derive::func3" << endl; }
> private:
> 	int d1;
> };
> 
> // void(*)() 定义一个函数指针变量
> typedef void(*VF_PTR)();
> 
> // 打印虚表
> void PrintVfTable(VF_PTR* pTable) {
> 	for (size_t i = 0; pTable[i] != 0; ++i) {
> 		printf("vfTable[%d]:%p->", i, *(int*)pTable[i]);
> 		VF_PTR f = pTable[i];
> 		f();
> 	}
> }
> 
> 
> int main()
> {
> 
> 	Derive d;
> 
> 	cout << sizeof(Derive) << endl;
> 
> 	PrintVfTable((VF_PTR*)*(int*)&d);
> 
> 	cout << endl;
> 	PrintVfTable((VF_PTR*)*(int*)((char*)&d + sizeof(Base1)));
> 
> 	return 0;
> }
> ```
>
> **多继承派生类的未重写的虚函数放在第一个继承基类部分的虚函数表中**
>
> ![image-20250211165507648](../../../../AppData/Roaming/Typora/typora-user-images/image-20250211165507648.png)